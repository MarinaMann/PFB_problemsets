#!/usr/bin/env python3

import sys

#using if/else and elif statements

x = 0
#x = sys.argv[1]

#function = bool(x)

if x > 0:
	message = 'is greater than 0'
	print(x. message)
elif x < 0:
	message = 'is less than 0'
	print(x, message)
else:
	message = 'x must be 0'
	print(x, message)


	
